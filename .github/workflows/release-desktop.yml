# Builds the tagged desktop release matrix and publishes the release assets.
name: release-desktop

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-desktop-${{ github.ref_name }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

env:
  FLUTTER_VERSION: "3.44.0"

jobs:
  cli:
    name: cli
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install CLI packaging helpers
        run: sudo apt-get update && sudo apt-get install -y zip

      - name: Build CLI release assets
        env:
          VERSION_NAME: ${{ github.ref_name }}
        run: |
          ./scripts/build_cli_packages.sh \
            --goos "${{ matrix.goos }}" \
            --goarch "${{ matrix.goarch }}" \
            --version "${VERSION_NAME#v}" \
            --output-dir dist/cli

      - name: Upload CLI assets
        uses: actions/upload-artifact@v4
        with:
          name: yunjuan-cli-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/cli/*
          if-no-files-found: error

  macos:
    name: macos-${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: macos-13
          - arch: arm64
            runner: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Enable macOS desktop
        run: flutter config --enable-macos-desktop

      - name: Build macOS release assets
        env:
          VERSION_NAME: ${{ github.ref_name }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          flutter pub get
          ./scripts/build_desktop_packages.sh \
            --platform macos \
            --arch "${{ matrix.arch }}" \
            --version "${VERSION_NAME#v}" \
            --build-number "$BUILD_NUMBER" \
            --output-dir dist/macos-${{ matrix.arch }}

      - name: Upload macOS assets
        uses: actions/upload-artifact@v4
        with:
          name: yunjuan-macos-${{ matrix.arch }}
          path: dist/macos-${{ matrix.arch }}/*
          if-no-files-found: error

  windows:
    name: windows-amd64
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install Windows amd64 toolchain
        uses: msys2/setup-msys2@v2
        with:
          update: true
          msystem: UCRT64
          install: >-
            mingw-w64-ucrt-x86_64-gcc

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Build Windows release assets
        env:
          VERSION_NAME: ${{ github.ref_name }}
          BUILD_NUMBER: ${{ github.run_number }}
          BRIDGE_CC: C:\msys64\ucrt64\bin\gcc.exe
        run: |
          flutter pub get
          ./scripts/build_desktop_packages.sh \
            --platform windows \
            --arch amd64 \
            --version "${VERSION_NAME#v}" \
            --build-number "$BUILD_NUMBER" \
            --output-dir dist/windows-amd64

      - name: Upload Windows assets
        uses: actions/upload-artifact@v4
        with:
          name: yunjuan-windows-amd64
          path: dist/windows-amd64/*
          if-no-files-found: error

  web:
    name: web-linux-${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goarch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Build Web release assets
        env:
          VERSION_NAME: ${{ github.ref_name }}
        run: |
          ./scripts/build_web_release.sh \
            --goarch "${{ matrix.goarch }}" \
            --version "${VERSION_NAME#v}" \
            --output-dir dist/web

      - name: Upload Web assets
        uses: actions/upload-artifact@v4
        with:
          name: yunjuan-web-linux-${{ matrix.goarch }}
          path: dist/web/*
          if-no-files-found: error

  linux:
    name: linux-${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install Linux desktop dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang \
            cmake \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu \
            libgtk-3-dev \
            liblzma-dev \
            ninja-build \
            pkg-config

      - name: Enable Linux desktop
        run: flutter config --enable-linux-desktop

      - name: Build Linux release assets
        env:
          VERSION_NAME: ${{ github.ref_name }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          flutter pub get
          if [[ "${{ matrix.arch }}" == "arm64" ]]; then
            export BRIDGE_CC=aarch64-linux-gnu-gcc
            export BRIDGE_CXX=aarch64-linux-gnu-g++
          fi
          ./scripts/build_desktop_packages.sh \
            --platform linux \
            --arch "${{ matrix.arch }}" \
            --version "${VERSION_NAME#v}" \
            --build-number "$BUILD_NUMBER" \
            --output-dir dist/linux-${{ matrix.arch }}

      - name: Upload Linux assets
        uses: actions/upload-artifact@v4
        with:
          name: yunjuan-linux-${{ matrix.arch }}
          path: dist/linux-${{ matrix.arch }}/*
          if-no-files-found: error

  publish:
    if: startsWith(github.ref, 'refs/tags/v')
    name: publish
    runs-on: ubuntu-latest
    needs:
      - cli
      - web
      - macos
      - windows
      - linux
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Mark scripts executable
        run: chmod +x scripts/*.sh

      - name: Download packaged artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/release
          merge-multiple: true

      - name: Generate release body
        env:
          TARGET_TAG: ${{ github.ref_name }}
          ASSET_DIR: dist/release
          OUTPUT_FILE: dist/release-body.md
        run: ./scripts/generate_release_body.sh

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: 云卷 ${{ github.ref_name }}
          body_path: dist/release-body.md
          files: dist/release/*
          fail_on_unmatched_files: true
